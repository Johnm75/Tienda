<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Tienda" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Primary colors -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryContainer">@color/colorPrimaryVariant</item>
        <item name="colorSecondary">@color/accent_pink</item>
        <item name="colorSecondaryContainer">@color/colorPrimaryVariant</item>

        <!-- Background and Surface colors -->
        <item name="background">@color/dark_background</item>
        <item name="colorSurface">@color/dark_gray</item>
        <item name="colorOnBackground">@color/light_gray</item>
        <item name="colorOnSurface">@color/light_gray</item>

        <!-- Text colors -->
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/light_gray</item>

        <!-- Button colors -->
        <item name="colorButtonNormal">@color/colorPrimary</item>
        <item name="android:colorAccent">@color/accent_pink</item>

        <!-- Status Bar and Navigation Bar -->
        <item name="android:statusBarColor">@color/dark_background</item>
        <item name="android:navigationBarColor">@color/dark_background</item>
    </style>

</resources>
